const current_age = 21;

const after_10_years = current_age + 10;

const name = "Quoc Bao";

const user = name + " , age: " + current_age;

console.log("After 10 years, " + name + " will be " + after_10_years + " years old.");

console.log("User info: " + user);

// Calculating area of a rectangle
const width = 10;
const height = 5;

const a = width * height;

console.log("The area of the rectangle is: " + a);
// Calculating the perimeter of a rectangle
const p = 2 * (width + height);

console.log("The perimeter of the rectangle is: " + p);

const isAdult = current_age >= 20 ? "is an adult" : "is not an adult";

console.log(name + " " + isAdult);